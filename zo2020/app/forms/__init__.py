''' FORMS

These are objects which present fields which can be edited. 

The data needs to then be processed:

If invalid, it uses error validation and shows the form again with errors pointed out
If valid, it processes the data according to the process_form() method







'''

from .public import *